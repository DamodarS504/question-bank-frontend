/**
 * src/pages/Login/LoginPage.jsx
 * Login page — delegates to the root-level LoginPage via re-export.
 * CSS is shared via pages/AuthPages.css (imported in root LoginPage).
 */
export { default } from '../LoginPage';
