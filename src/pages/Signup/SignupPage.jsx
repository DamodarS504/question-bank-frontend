/**
 * src/pages/Signup/SignupPage.jsx
 * Signup page — delegates to the root-level SignupPage via re-export.
 * CSS is shared via pages/AuthPages.css (imported in root SignupPage).
 */
export { default } from '../SignupPage';
